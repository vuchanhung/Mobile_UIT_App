<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BigProject"
        tools:targetApi="31">
        <activity
            android:name=".Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.login">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity android:name=".Activity.logo">
            <!-- Thêm các thuộc tính khác nếu cần -->
        </activity>
        <activity
            android:name=".Activity.Setting_Activity">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.Semester">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.setting_logout">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.Subject">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.Chat">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.SubjectDetail">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.Schedule">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.Diemdanh">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.Home">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.NewsActivity">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.DetailNewsActivity">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
        <activity
            android:name=".Activity.EventEditActivity">
            <!-- Thêm các cài đặt khác cho LogoActivity nếu có -->
        </activity>
    </application>

</manifest>